# Technical note

### order cmd to start project
1. ```uv init```
2. ```uv venv```

### installed packages:
1. uv pip install sentence-transformers torch
2. uv pip install hf_xet
3. uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
